// This script is currently empty, but you can add functionality here later,
// such as copying the coupon code to the user's clipboard.

// Example: copying coupon code on button click (optional)
/*
const copyButtons = document.querySelectorAll('.btn-primary');

copyButtons.forEach(button => {
  button.addEventListener('click', () => {
    const codeText = button.parentElement.querySelector('.card-text').textContent;
    navigator.clipboard.writeText(codeText);
    alert('Código copiado!');
  });
});
*/
